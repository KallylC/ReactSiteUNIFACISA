{
  "name": "meu-projeto-faculdade-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend & npm run dev --prefix backend",
    "install-all": "npm install && npm install --prefix frontend && npm install --prefix backend",
    "start": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
